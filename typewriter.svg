<svg viewBox="0 0 800 100" xmlns="http://www.w3.org/2000/svg">
  <style>
    .text {
      font: bold 24px monospace;
      fill: white;
    }
    .cursor {
      fill: white;
      animation: blink 1s steps(2, start) infinite;
    }
    @keyframes blink {
      to {
        visibility: hidden;
      }
    }
    @keyframes typing {
      from {
        width: 0;
      }
      to {
        width: 30ch;
      }
    }
    @keyframes erasing {
      from {
        width: 30ch;
      }
      to {
        width: 0;
      }
    }
    .typing {
      overflow: hidden;
      border-right: 0.15em solid white;
      white-space: nowrap;
      animation: 
        typing 4s steps(30, end),
        erasing 4s steps(30, end) 4s,
        typing 4s steps(62, end) 8s,
        erasing 4s steps(62, end) 12s;
      animation-iteration-count: infinite;
    }
  </style>
  <rect x="0" y="5" width="10" height="30" class="cursor"/>
  <text x="0" y="30" class="text typing">Welcome to my profile</text>
  <text x="0" y="30" class="text typing" style="visibility: hidden;">| ➜ Kernel Developer | ➜ Discord bot Developer | ➜ Software Designer |</text>
</svg>
